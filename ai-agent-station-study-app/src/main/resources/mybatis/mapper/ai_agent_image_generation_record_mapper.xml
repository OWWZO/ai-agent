<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.wwz.ai.domain.agent.reactor.mapper.IAgentImageGenerationRecordDao">

    <resultMap id="AgentImageGenerationRecordMap" type="org.wwz.ai.domain.agent.reactor.entity.AgentImageGenerationRecord">
        <id column="id" property="id"/>
        <result column="request_id" property="requestId"/>
        <result column="result_index" property="resultIndex"/>
        <result column="device_id" property="deviceId"/>
        <result column="user_id" property="userId"/>
        <result column="prompt" property="prompt"/>
        <result column="mode" property="mode"/>
        <result column="size" property="size"/>
        <result column="batch_count" property="batchCount"/>
        <result column="source_image_count" property="sourceImageCount"/>
        <result column="mask_image_count" property="maskImageCount"/>
        <result column="used_fallback" property="usedFallback"/>
        <result column="file_name" property="fileName"/>
        <result column="oss_url" property="ossUrl"/>
        <result column="domain_url" property="domainUrl"/>
        <result column="download_url" property="downloadUrl"/>
        <result column="file_size" property="fileSize"/>
        <result column="mime_type" property="mimeType"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <insert id="insert" parameterType="org.wwz.ai.domain.agent.reactor.entity.AgentImageGenerationRecord" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_agent_image_generation_record (
            request_id, result_index, device_id, user_id,
            prompt, mode, size, batch_count, source_image_count, mask_image_count, used_fallback,
            file_name, oss_url, domain_url, download_url, file_size, mime_type,
            create_time, update_time, deleted
        ) VALUES (
            #{requestId}, #{resultIndex}, #{deviceId}, #{userId},
            #{prompt}, #{mode}, #{size}, #{batchCount}, #{sourceImageCount}, #{maskImageCount}, #{usedFallback},
            #{fileName}, #{ossUrl}, #{domainUrl}, #{downloadUrl}, #{fileSize}, #{mimeType},
            now(), now(), 0
        )
    </insert>

    <select id="countDistinctRequestIdByDeviceId" resultType="int">
        SELECT COUNT(DISTINCT request_id)
        FROM ai_agent_image_generation_record
        WHERE device_id = #{deviceId} AND deleted = 0
    </select>

    <select id="queryRequestIdsByDeviceId" resultType="java.lang.String">
        SELECT request_id
        FROM ai_agent_image_generation_record
        WHERE device_id = #{deviceId} AND deleted = 0
        GROUP BY request_id
        ORDER BY MAX(create_time) DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="queryByRequestIds" resultMap="AgentImageGenerationRecordMap">
        SELECT *
        FROM ai_agent_image_generation_record
        WHERE device_id = #{deviceId}
          AND deleted = 0
          AND request_id IN
        <foreach collection="requestIds" item="requestId" open="(" separator="," close=")">
            #{requestId}
        </foreach>
        ORDER BY create_time DESC, result_index ASC
    </select>

</mapper>
