## 1. Workspace Routing And Entry

- [x] 1.1 在 `ui/src/router/index.tsx` 中新增 `/workspace/mrag` 路由，并懒加载 MRAG Workspace 页面。
- [x] 1.2 抽离或补充 Workspace 内部切换入口，保证用户可以在前端显式进入 MRAG Workspace，同时保留现有图片生成 Workspace 的可达性。
- [x] 1.3 搭建 `WorkspaceMRag` 页面骨架与基础类型/工具函数，包含工具服务地址、当前知识库和页面局部状态的初始化逻辑。

## 2. MRAG Service Layer

- [x] 2.1 新增 MRAG Workspace 服务模块，封装知识库列表、创建知识库、文件列表、删除文件、网页链接入库等 `reactor-tool` 文档管理接口。
- [x] 2.2 实现本地文件“两段式”上传流程：先调用 `/v1/documents/upload`，再调用 `/v1/documents/add_files` 发起入库任务。
- [x] 2.3 实现上传结果 URL 归一化策略，优先把可预览地址写入知识库文件记录，并为文件列表生成预览/下载访问地址。
- [x] 2.4 实现文件状态轮询与错误映射工具，仅在存在 `PENDING` 或 `RUNNING` 文件时刷新当前知识库文件列表。

## 3. Knowledge Base And File Workspace UI

- [x] 3.1 实现知识库列表区域，支持加载知识库、切换当前知识库和创建新知识库。
- [x] 3.2 实现文件入库区域，支持本地文件上传和网页链接提交，并在提交后刷新当前知识库文件列表。
- [x] 3.3 实现文件列表区域，展示文件名称、来源类型、处理状态和必要的时间/数量信息。
- [x] 3.4 为文件列表增加原始资料访问入口，支持上传文件预览/下载，以及网页来源原链接打开。
- [x] 3.5 实现文件删除、空态、失败态和刷新反馈，保证文件级管理闭环完整。

## 4. Retrieval Debugger

- [x] 4.1 新增 `mragQuery` 的 SSE 客户端封装，支持把当前选中知识库 `kb_id` 和用户问题一起发送到 `reactor-tool`。
- [x] 4.2 实现检索调试区表单与请求状态管理，支持发起查询、处理中状态提示和异常提示。
- [x] 4.3 实现流式回答展示区，持续渲染增量内容并在结束后呈现最终回答结果。
- [x] 4.4 为返回结果补充 Markdown/链接/图片等可视化渲染，明确区分“回答结果”与“不提供 chunk 明细”的首期边界。

## 5. Verification And Hardening

- [x] 5.1 为 MRAG 服务层补充关键单测或工具函数校验，覆盖 URL 归一化、状态轮询触发条件和错误映射。
- [x] 5.2 为 Workspace 关键交互补充页面级验证，覆盖知识库切换、文件入库状态刷新、原始文件访问和检索调试主流程。
- [x] 5.3 执行 `ui` 相关 lint/build 验证，并整理需要在实现阶段一并修复的兼容性问题。
